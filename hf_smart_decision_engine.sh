#!/bin/bash
echo "🧠 محرك القرار الذكي - مدير المصنع 🚀"
echo "==================================="

analyze_task() {
    local task="$1"
    echo "🔍 تحليل المهمة: '$task'"
    
    if [[ "$task" == *"خطأ"* ]] || [[ "$task" == *"error"* ]] || [[ "$task" == *"bug"* ]]; then
        echo "🎯 القرار: توجيه إلى Debug Expert"
        echo "🔧 السبب: تحتاج تحليل أخطاء متخصص"
        echo "💡 التنفيذ: ./hf_run_debug_expert.sh '$task'"
        
    elif [[ "$task" == *"مشروع"* ]] || [[ "$task" == *"تصميم"* ]] || [[ "$task" == *"بنية"* ]]; then
        echo "🎯 القرار: توجيه إلى System Architect" 
        echo "🏗️ السبب: تحتاج تصميم نظام أو بنية"
        echo "💡 التنفيذ: ./hf_run_system_architect.sh '$task'"
        
    elif [[ "$task" == *"تعلم"* ]] || [[ "$task" == *"تدريب"* ]] || [[ "$task" == *"مسار"* ]]; then
        echo "🎯 القرار: توجيه إلى Technical Coach"
        echo "👨‍🏫 السبب: تحتاج بناء مسار تعلمي"
        echo "💡 التنفيذ: ./hf_run_technical_coach.sh '$task'"
        
    elif [[ "$task" == *"معلومة"* ]] || [[ "$task" == *"بحث"* ]] || [[ "$task" == *"وثيقة"* ]]; then
        echo "🎯 القرار: توجيه إلى Knowledge Spider"
        echo "🕷️ السبب: تحتاج جمع معلومات"
        echo "💡 التنفيذ: ./hf_run_knowledge_spider.sh '$task'"
        
    else
        echo "🎯 القرار: توجيه إلى Debug Expert (افتراضي)"
        echo "🔧 السبب: تحليل عام للمهمة"
        echo "💡 التنفيذ: ./hf_run_debug_expert.sh '$task'"
    fi
}

# اختبار النظام
if [ -n "$1" ]; then
    analyze_task "$1"
else
    echo "💡 أمثلة على الاستخدام:"
    echo "  $0 'هناك خطأ في الكود'"
    echo "  $0 'أريد تصميم مشروع جديد'"
    echo "  $0 'ابني لي مسار تعلم برمجة'"
    echo "  $0 'ابحث لي عن معلومات عن الذكاء الاصطناعي'"
fi
